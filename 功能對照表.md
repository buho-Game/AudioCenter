# AudioCenter 功能對照表（規劃 vs. 實作）

> 對照 [文件.md](文件.md) / [設計師教學.md](設計師教學.md) 中的規劃，逐項核對目前程式碼是否已實作。
> 核對日期：2026-06-10。✅＝已實作（附程式碼出處）。

---

## 一、改善規劃對照（文件 Section 二）

| 規劃項目 | 狀態 | 程式碼佐證 |
| --- | --- | --- |
| 完全獨立、隨拿隨用（純 Unity、無外部依賴） | ✅ | `Runtime/AudioCenter.asmdef` 的 `references` 為空 |
| 一個總窗口管所有聲音 | ✅ | `AudioCenterAudioManager`（Runtime/Core） |
| 播放器重複使用，不浪費 | ✅ | `AudioCenterSoundPool` 的 `Voice` + `GetFreeVoice()`（Runtime/Core） |
| 音量分四種，各自能靜音 | ✅ | `AudioCenterVolumeBus` = Master/BGM/SFX/UI；`SetBusMute(bus, mute)` |
| 任何一種音量都能慢慢變大小聲 | ✅ | `FadeVolume(bus, target, duration)`（不再只限音樂） |
| 音樂進階控制（播放/停止/暫停/繼續/淡入淡出） | ✅ | `PlayBGM / StopBGM / PauseBGM / ResumeBGM / FadeInBgm / FadeOutBgm` |
| 旁白時自動壓低背景音樂（duck） | ✅＊ | `SetBgmDuckMultiplier / ClearBgmDuckMultiplier`；序列器 `BgmDuckStep / BgmUnduckStep` |
| 音效序列器 | ✅ | `AudioCenterAudioSequencer` + 16 種 Step（Bgm/Sound/Volume/Flow） |
| 隨機環境音（整併舊兩零件） | ✅ | `AudioCenterAmbientAudio`（註解：Merges RepeatAudioPlayer + RandomPlay） |
| 聲音通訊錄（依名字呼叫） | ✅ | `AudioCenterClipLibrary`：`this[string groupName]` |
| 不用寫程式也能用（設計師面板操作） | ✅ | Inspector 元件 + 序列器 Step + 編輯器視窗 `AudioCenter ▸ Audio Library` |

＊ duck 機制已具備，由內容端在旁白前後觸發（程式或序列器），而非偵測「有聲音在播」自動觸發。

---

## 二、分層音樂混音器 AudioCenterMusicMixer（文件 Section 三）

| 規劃項目 | 狀態 | 程式碼佐證 |
| --- | --- | --- |
| 底層常駐 + 可切換層 | ✅ | `mainBgm` + `List<MusicLayer> layers` |
| 所有層一起播、對齊同拍 | ✅ | 共用 DSP 起始時間 `PlayScheduled(startDsp)`，sample-accurate phase-lock |
| 切換＝淡進淡出音量、時間軸不重來 | ✅ | 只 crossfade gain，timeline 不重啟 |
| 「主頁」純底層狀態可選 | ✅ | `SwitchToMainPage()` + `bedOnlyVolume` |
| 音量自動跟隨玩家音樂／總音量 | ✅ | mixer 乘上 music + master 音量 |

> 設計前提：各層需為「同一首曲子拆出的分軌」（等長、等速），否則會在循環邊界 drift。此為使用前提，與文件一致。

---

## 結論

舊系統 6 個痛點（散裝腳本、用完即丟、音量分類太少、只有音樂能淡入淡出、音樂單軌會斷、進階效果要手接）在 AudioCenter 中**全部對應到已實作的功能，無遺漏**。
