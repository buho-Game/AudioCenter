# AudioCenter Audio

> **這份文件寫給：完全不會寫程式的設計師 / 企劃 / 美術。**
> 你只需要在 Unity 的 **Inspector 面板**裡拖、拉、點選，就能讓遊戲發出聲音。
> 全程**不需要寫任何一行程式碼**。
>
> （如果你是工程師，想看 API 與架構，請改看同資料夾的 [`文件.md`](文件.md)。）
>
> 📂 套件位置：`Assets/Plugins/AudioCenterAudio/`

---

## 目錄

1. [先搞懂三個名詞](#1-先搞懂三個名詞)
2. [第一步：場景裡要有一個「音效管理員」](#2-第一步場景裡要有一個音效管理員)
3. [第二步：把你的音檔登記到「音效庫」](#3-第二步把你的音檔登記到音效庫)
4. [第三步：讓某個物件發出聲音（最常用）](#4-第三步讓某個物件發出聲音最常用)
5. [第四步：讓 UI 按鈕發出聲音](#5-第四步讓-ui-按鈕發出聲音)
6. [進階：用「音效序列器」排一段聲音演出](#6-進階用音效序列器排一段聲音演出)
7. [進階：分層音樂混音器（同步疊加音樂）](#7-進階分層音樂混音器同步疊加音樂) 🆕
8. [做一個「音量設定」面板](#8-做一個音量設定面板)
9. [其他好用的現成元件](#9-其他好用的現成元件)
10. [常見問題 FAQ](#10-常見問題-faq)
11. [名詞速查表](#11-名詞速查表)

---

## 1. 先搞懂三個名詞

整套系統只圍繞三種聲音，先記住它們：

| 名詞                | 白話解釋                                                       | 比喻             |
| ------------------- | -------------------------------------------------------------- | ---------------- |
| **BGM**（背景音樂） | 通常會一直循環播放的音樂，例如主選單音樂、戰鬥音樂             | 餐廳裡放整天的歌 |
| **SFX**（音效）     | 短短一下就結束的聲音，例如點擊、爆炸、跳躍                     | 門鈴「叮咚」一聲 |
| **UI**（介面音）    | 按鈕、選單發出的聲音，本質上也是音效，只是另外給它一個音量旋鈕 | 手機按鍵聲       |

> 每一種聲音都有自己的**音量旋鈕**，玩家可以分開調整音樂大小、音效大小。這就是為什麼要分類。

---

## 2. 第一步：場景裡要有一個「音效管理員」

整個場景需要**一個**管理員物件負責放聲音。多數情況下這個已經幫你做好了——先檢查有沒有。

### 怎麼檢查 / 怎麼建立

1. 在場景的 **Hierarchy**（左邊那欄物件清單）找找看有沒有叫 **AudioManager** 之類的物件。
2. **有的話** → 你不用做任何事，直接跳到[第三步](#4-第三步讓某個物件發出聲音最常用)。
3. **沒有的話**，自己建一個：
   - 在 Hierarchy 空白處按右鍵 → **Create Empty**，把它改名叫 `AudioManager`。
   - 選中它，在右邊 **Inspector** 按 **Add Component**。
   - 搜尋並加入 **`audioCenter/audioCenterAudioManager`**。

### 加完之後要填兩格

選中 AudioManager，Inspector 會出現兩個欄位，把對應的資產拖進去：

| 欄位                             | 拖什麼進去                                           |
| -------------------------------- | ---------------------------------------------------- |
| **Library**（音效庫）            | 專案裡的音效庫資產（Clip Library，下一步會教怎麼做） |
| **Default Settings**（預設設定） | 專案裡的音效設定資產（Game Settings）                |

> 💡 在 **Project** 視窗（下方檔案總管）上方的搜尋框打關鍵字（例如 `ClipLibrary`、`AudioSettings`）就能找到資產，直接從搜尋結果拖到欄位上。
>
> 找不到現成的設定資產？在 Project 視窗空白處按右鍵 → **Create → AudioCenter → Audio → Game Settings** 就能新建一個，裡面可以調預設音量。

> ✅ 這個管理員會**自動跨場景保留**，所以整個遊戲只要設定一次。

---

## 3. 第二步：把你的音檔登記到「音效庫」

「音效庫」（Clip Library）就是一本**聲音通訊錄**：你把每個音檔取一個好記的名字，之後到處都用名字呼叫它，不用每次去翻檔案。

### 建立 / 打開音效庫

- **新建**：在 Project 視窗空白處按右鍵 → **Create → AudioCenter → Audio → Clip Library**，取個名字（例如 `MyClipLibrary`）。
- **打開既有的**：在 Project 視窗點一下音效庫資產，設定會顯示在右邊 Inspector。

打開後會看到一個個「群組（Group）」，例如 `BGM`、`SFX`、`Combat`。

### 加一個新聲音

1. 找到要放進去的群組（音樂放 `BGM`，音效放 `SFX`）。沒有合適的群組可以自己新增一個。
2. 在該群組的清單按 **`+`** 多一列。
3. 一列有兩格：
   - **Clip Name**：自己取的名字，例如 `MainMenu`、`ButtonClick`、`Explosion`。**這個名字之後要用到，取好記一點。**
   - **Clip**：把你的音檔（`.wav` / `.mp3` / `.ogg`）從 Project 視窗拖進來。

> 📌 之後所有地方呼叫聲音，都是用「**群組名 + 名字**」這個組合，例如：群組 `SFX`、名字 `ButtonClick`。記住這個觀念，下面會一直用到。

> 💡 群組只是分類抽屜，方便整理。你可以自由增加群組（例如 `Boss`、`Ambient`）。
> 別忘了把做好的音效庫拖回[第一步](#2-第一步場景裡要有一個音效管理員) AudioManager 的 **Library** 欄位。

---

## 4. 第三步：讓某個物件發出聲音（最常用）

這是**最核心、最常用**的做法。不需要工程師、不用寫程式：在任何物件上掛一個 **audioCenterAudioController** 元件，設定好「要放什麼聲音」，再決定「什麼時候放」。

### 步驟

1. 選中你想讓它發出聲音的物件（任何 GameObject 都行）。
2. **Add Component** → 搜尋並加入 **`audioCenter/audioCenterAudioController`**。
3. 你會看到這些選項，照下面填：

#### A. 要放音樂還是音效？

- **Type** 選 **Sound**（音效）或 **BGM**（背景音樂）。

#### B. 這個聲音從哪裡來？

- **Clip Reference Type**：
  - 選 **Library** → 然後填 **Group Name**（群組名，例如 `SFX`）和 **Clip Name**（名字，例如 `ButtonClick`）。**← 推薦這個。**
  - 選 **File** → 直接把音檔拖到 **Clip** 欄位。（適合臨時測試）

#### C. 要做什麼動作？

- 如果是 **Sound**：**Sound Action Type** 通常選 **Play**（播放）。其他選項：Stop（停止）、Stop All（全部停止）。
- 如果是 **BGM**：**Bgm Action Type** 可選 **Play / Stop / Pause / Resume / Fade In / Fade Out**。

#### D. 一些好用的小開關

| 選項                      | 作用                                                                             |
| ------------------------- | -------------------------------------------------------------------------------- |
| **Loop**                  | 打勾 = 一直重複播放（音樂通常要勾，短音效不勾）                                  |
| **Track**                 | 這個音效歸到哪一條音軌（SFX / UI），決定它受哪個音量旋鈕控制                     |
| **Rnd Pitch**（隨機音高） | 打勾後同一個音效每次播放音調會微微不同，避免聽起來很機械（例如連續開槍、撿金幣） |
| **Rnd Range**             | 配合上面，控制音調變化的範圍                                                     |

### 什麼時候會發出聲音？

設定好聲音後，要決定「**觸發時機**」，有兩種：

1. **一進場就自動播** → 把元件上的 **Action On Start** 打勾。
   - 適合：場景背景音樂、進場環境聲。

2. **由某個事件觸發**（按鈕、碰撞、動畫…）→ 不要勾 Action On Start，改用 **UnityEvent** 呼叫這個元件的 **`DoAction()`**。
   - 下一節的按鈕範例就是用這個方式，看下去就懂。

---

## 5. 第四步：讓 UI 按鈕發出聲音

最經典的需求：「按鈕被點的時候叮一聲」。完全在 Inspector 裡接好，不寫程式。

1. 在按鈕物件上加 **`audioCenter/audioCenterAudioController`** 元件，照[第三步](#4-第三步讓某個物件發出聲音最常用)設定好你的點擊音效（Type = Sound、Library、群組 `UI`、名字例如 `Click`、動作 Play）。**不要**勾 Action On Start。
2. 選中按鈕，找到它的 **Button** 元件，最下面有一塊 **On Click ()**。
3. 按 **`+`** 多一列。
4. 把**這顆按鈕自己**（或掛了 audioCenterAudioController 的物件）拖到那一列左下角的物件欄。
5. 右邊的下拉選單選 **`AudioCenterAudioController → DoAction ()`**。

完成！現在每次點這顆按鈕，就會播放你設定的音效。

> 💡 同樣的「On Click → DoAction」做法，也能用在任何有 UnityEvent 的地方：碰撞觸發器、Timeline 訊號、動畫事件等等。

---

## 6. 進階：用「音效序列器」排一段聲音演出

當你想要**一連串、有先後順序**的聲音演出時，用 **audioCenterAudioSequencer**（音效序列器）。它像一張「**聲音的時間表**」，你一格一格排好，它就照順序自動跑。

**典型情境：** 過場時——先把音樂淡出 → 停掉音樂 → 播一個轉場音效 → 等它播完 → 換上新場景的音樂。

### 怎麼用

1. 建一個空物件（或用現有的），**Add Component** → **`audioCenter/audioCenterAudioSequencer`**。
2. 你會看到一個 **Audio Steps**（步驟）清單，按 **`+`** 加步驟。每按一次會跳出選單，從裡面挑步驟類型（見下表）。
3. 由上而下排好順序就完成了。

### 上面那兩個開關

| 開關              | 作用                                  |
| ----------------- | ------------------------------------- |
| **Play On Start** | 打勾 = 一進場就自動開始跑這張時間表   |
| **Loop**          | 打勾 = 整張時間表跑完後，從頭再跑一次 |

> 測試用：Inspector 下方有 **Play / Stop** 兩個按鈕，可以在編輯器裡直接試聽整段演出。

### 可以加哪些步驟

從 `+` 選單會看到這些（用「分類/動作」呈現）：

**🎵 BGM（背景音樂）**

- `BGM/Play`：播放音樂　|　`BGM/Stop`：停止　|　`BGM/Pause`：暫停　|　`BGM/Resume`：繼續
- `BGM/Fade In`：漸漸變大聲　|　`BGM/Fade Out`：漸漸變小聲後停止

**🔊 Sound（音效）**

- `Sound/Play`：播放音效　|　`Sound/Stop`：停止某個音效　|　`Sound/Stop All`：停掉全部音效

**🎚️ Volume（音量）**

- `Volume/Set`：直接設定某個音量　|　`Volume/Mute`：靜音/取消靜音　|　`Volume/Fade`：把音量慢慢拉到某個值

**⏱️ Flow（流程控制）**

- `Flow/Wait`：在這裡等幾秒
- `Flow/Wait For BGM`：等到目前音樂播完才繼續
- `Flow/BGM Duck`：把音樂壓小聲（例如旁白要講話時）　|　`Flow/BGM Unduck`：把音樂恢復原本大小
- `Flow/Event`：在這個時間點觸發一個 UnityEvent（拿來通知其他系統）

### 每個步驟都有的共通設定

| 欄位                    | 作用                                                               |
| ----------------------- | ------------------------------------------------------------------ |
| **Label**               | 給這步驟取個名字，方便你自己看（選填）                             |
| **Active**              | 取消打勾 = 暫時跳過這一步（除錯很好用）                            |
| **Initial Delay**       | 執行這步**之前**先等幾秒                                           |
| **Wait For Completion** | 打勾 = 序列器會**等這一步做完**才往下一步；不勾 = 觸發後立刻往下走 |

> 💡 **「等待類」步驟**（`Flow/Wait`、`Flow/Wait For BGM`）本來就會擋住流程，不用管 Wait For Completion。
> 想要「播一個音效，等它整個放完再做下一件事」→ 在那個 `Sound/Play` 步驟把 **Wait For Completion 打勾**。

---

## 7. 進階：分層音樂混音器（同步疊加音樂）🆕

**audioCenterMusicMixer** 解決一個很常見的音樂需求：**同一首音樂，依遊戲狀態疊加不同的層次，而且切換時不會跳拍、不會重頭播。**

**典型情境：**

- 平時只放「基底音樂」（鼓 + 貝斯）；進入戰鬥時，平順地疊上「弦樂層」；Boss 出現再換成「銅管層」。
- 玩家在不同區域移動時，音樂氣氛跟著變，但**節奏始終對齊、無縫銜接**。

它的原理：所有層次（包含基底）都**同時、對齊同一個拍點開始並各自循環**，切換層次時只是把某一層的音量淡入、其他層淡出——音樂時間軸從不重來，所以聽起來永遠在同一個拍子上。

> 🎼 **重要前提：** 所有層次的音檔最好是**同一首曲子拆出來的分軌（stems）**，長度、速度（BPM）都要一樣。長度不同的音檔會在循環邊界逐漸跑掉拍子。

### 怎麼設定（全程 Inspector）

1. 建一個空物件，**Add Component** → **`audioCenter/audioCenterMusicMixer`**。
2. 填這些欄位：

| 欄位                   | 意思                                                                                                                          |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| **Main Bgm**           | 基底音樂——永遠墊在最底下持續播放的那一層                                                                                      |
| **Bed Volume**         | 基底音樂的音量（0～1）                                                                                                        |
| **Layers**             | 可切換的「第二音樂」層清單。每一層有：**Name**（自取名字，例如 `Combat`、`Boss`）、**Clip**（音檔）、**Volume**（這層的音量） |
| **Start Layer**        | 一開始要讓哪一層發聲（填層的編號 0、1、2…；填 **-1** = 只放基底，不疊任何層）                                                 |
| **Play On Start**      | 打勾 = 一進場就自動開始                                                                                                       |
| **Crossfade Duration** | 切換層次時的淡入淡出秒數（0 = 直接硬切）                                                                                      |
| **Schedule Lead Time** | 開始前的預備時間，讓所有層確實一起起跑（保持預設 0.1 即可）                                                                   |

> ✅ 這個混音器會**自動跟著玩家的音樂音量、主音量**走，所以玩家在設定面板調音樂大小一樣有效。

### 怎麼切換層次（不用寫程式）

**方法 A：先在編輯器裡試聽（最快）**

1. 在同一個物件再 **Add Component** → **`audioCenter/audioCenterMusicMixerDemo`**。
2. 進入 **Play 模式**，在 Inspector 對著這個元件的標題**按右鍵**，會跳出測試選單：
   - ▶ Play（基底 + 起始層）／■ Stop
   - Switch ▸ Layer 0 / 1 / 2、Next layer、Previous layer
3. 每點一次，Console 會印出目前狀態（前綴 `[MusicMixer]`），方便確認有沒有切成功。

**方法 B：由遊戲事件觸發（正式做法）**
用任何 UnityEvent（按鈕 On Click、觸發器、動畫事件…）呼叫 audioCenterMusicMixer 上的這些函式：

| 函式                              | 作用                                 |
| --------------------------------- | ------------------------------------ |
| **`Play ()`**                     | 開始播放                             |
| **`Stop ()`**                     | 停止並清乾淨                         |
| **`SwitchTo (int)`**              | 淡入切換到第幾層（傳 -1 = 只留基底） |
| **`Next ()`** / **`Previous ()`** | 切到下一層 / 上一層                  |
| **`MuteLayers ()`**               | 把所有疊加層淡出，只留基底           |

> 例如：在「進入戰鬥區」的觸發器上接 `On Trigger → SwitchTo`，參數填 `1`，玩家一踏進去音樂就無縫疊上戰鬥層。

> 💡 **跟序列器/BGM 的差別：** [第六步](#6-進階用音效序列器排一段聲音演出)的 BGM 適合「整首換掉」的音樂；這裡的混音器適合「同一首、依狀態加減層次」的動態音樂。兩者可以並用。

---

## 8. 做一個「音量設定」面板

想讓玩家在設定頁面拖動滑桿調整音樂/音效大小？用現成的 **audioCenterAudioSettings** 元件。

1. 在你的設定面板物件上 **Add Component** → **`audioCenter/audioCenterAudioSettings`**。
2. 它有三個欄位，把你做好的 UI 滑桿（Slider）拖進對應的格子：
   - **Music Volume Slider** → 音樂音量滑桿
   - **Sfx Volume Slider** → 音效音量滑桿
   - **Ui Volume Slider** → 介面音量滑桿
3. 完成！滑桿會自動讀取目前音量、玩家拖動時也會即時生效，**不用自己接任何 On Value Changed**。

---

## 9. 其他好用的現成元件

需要時再用，加元件的方式都一樣（Add Component → 搜尋名字）：

| 元件名稱                       | 用途                                                       | 怎麼用                                                                                                                                         |
| ------------------------------ | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| **`audioCenter/audioCenterMusicMixer`**      | **分層音樂混音器**：同步疊加、無縫切換音樂層次             | 見[第七步](#7-進階分層音樂混音器同步疊加音樂)的完整說明                                                                                        |
| **`audioCenter/audioCenterAmbientAudio`**    | **隨機環境音**：每隔幾秒隨機放一個聲音（鳥叫、風聲、滴水） | 在 **Audio Pool** 清單裡加入幾個聲音設定，設定 **Repeat Interval**（間隔秒數的最小/最大值），由事件呼叫 `DoAction()` 開始、`StopAction()` 停止 |

> 這幾個比較進階，遇到再回來看就好。

---

## 10. 常見問題 FAQ

**Q：我照做了，可是沒聲音？**

1. 場景裡有沒有 **AudioManager**？（[第二步](#2-第一步場景裡要有一個音效管理員)）
2. AudioManager 的 **Library** 和 **Default Settings** 兩格有沒有填？
3. 音效庫裡的 **Clip Name / Group Name** 有沒有打錯字？大小寫要一致。
4. 電腦/Unity 沒靜音吧？音量設定不是 0 吧？

**Q：音樂一直重頭播 / 只播一次就停？**
→ 看 **Loop** 有沒有打勾。音樂要勾、短音效不要勾。

**Q：分層音樂切換時聽起來跑拍 / 不對齊？**
→ 確認每一層的音檔是**同一首曲子拆出來的分軌**，長度與 BPM 都一致（見[第七步](#7-進階分層音樂混音器同步疊加音樂)的前提）。長度不同會在循環時逐漸跑掉。

**Q：同一個音效連續觸發時聽起來很煩？**
→ 在 audioCenterAudioController 勾 **Rnd Pitch**（隨機音高），每次音調會稍微不同，自然很多。

**Q：旁白講話時想把背景音樂壓小聲？**
→ 用序列器的 `Flow/BGM Duck`（壓小）和 `Flow/BGM Unduck`（恢復），中間夾你的旁白。

**Q：我可以參考一個做好的範例嗎？**
→ 打開場景 `Assets/Scenes/Labs/Music.unity`，裡面有完整的示範擺法（含分層音樂混音器）。

---

## 11. 名詞速查表

| 名詞                      | 意思                                                            |
| ------------------------- | --------------------------------------------------------------- |
| **Inspector**             | Unity 右邊那欄，顯示選中物件的所有設定                          |
| **Hierarchy**             | 左邊那欄，列出場景裡所有物件                                    |
| **Project**               | 下方的檔案總管，放所有資產（音檔、資產檔等）                    |
| **Component（元件）**     | 掛在物件上、賦予它能力的小模組。用 **Add Component** 加         |
| **Group（群組）**         | 音效庫裡的分類抽屜，例如 BGM、SFX                               |
| **Clip Name（剪輯名）**   | 你給每個音檔取的好記名字                                        |
| **Loop（循環）**          | 重複播放                                                        |
| **Fade（淡入/淡出）**     | 聲音慢慢變大 / 變小                                             |
| **Crossfade（交叉淡化）** | 一邊淡出、另一邊同時淡入，平順地換聲音                          |
| **Layer（層 / 分軌）**    | 同一首音樂中可以個別開關的一條軌，例如鼓、弦樂、銅管            |
| **Stems（分軌素材）**     | 同一首曲子拆成多個檔案的版本，長度與速度一致，用來疊層          |
| **Duck（閃避）**          | 暫時把音樂壓小聲，讓其他聲音（如旁白）突出                      |
| **Sequencer（序列器）**   | 一張「聲音時間表」，照順序自動播放一連串動作                    |
| **UnityEvent**            | Inspector 裡可以「按下時做某件事」的接線板（例如按鈕 On Click） |

---

**就這樣！** 90% 的需求只要用到[第二步](#2-第一步場景裡要有一個音效管理員)～[第五步](#5-第四步讓-ui-按鈕發出聲音)。
要動態音樂看[第七步](#7-進階分層音樂混音器同步疊加音樂)，複雜的聲音演出看[第六步](#6-進階用音效序列器排一段聲音演出)。祝你做出好聽的遊戲 🎧
