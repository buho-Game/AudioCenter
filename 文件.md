# AudioCenter Audio 音效系統文件

> **套件名稱：** `com.audiocenter.audio`
> **版本：** 1.0.0
> **相依套件：** 無（純 Unity AudioSource，無第三方相依）

---

## 目錄

1. [架構概覽](#架構概覽)
2. [快速開始](#快速開始)
3. [核心元件](#核心元件)
4. [音效庫系統](#音效庫系統)
5. [互動音效層](#互動音效層)
6. [音效設定 UI](#音效設定-ui)
7. [編輯器工具](#編輯器工具)
8. [API 速查表](#api-速查表)
9. [音效剪輯對照表](#音效剪輯對照表)

---

## 架構概覽

```
[ 遊戲程式碼 / UI / AudioCenterMusicMixer ]
                ↓
       [ AudioCenterAudioManager ]          ← 統一公開 API 入口
                ↓
       [ AudioCenterSoundPool ]
        BGM／SFX／UI 音軌
```

- **AudioCenterAudioManager** — 所有遊戲直接呼叫的門面（Facade），負責統一的音效控制
- **AudioCenterSoundPool** — 內建原生音源池，負責 AudioSource 池化與音軌音量／靜音處理

---

## 快速開始

### 步驟一：建立場景音效管理器

1. 在場景中建立空白物件，命名為 `AudioCenterAudioManager`
2. 掛載 `AudioCenterAudioManager` 元件
3. 將 `DefaultClipLibrary.asset` 指派至 **Library** 欄位
4. 將 `AudioCenterAudioSettingsSO` 指派至 **Default Settings** 欄位

### 步驟二：播放背景音樂

```csharp
// 從音效庫播放（建議方式）
AudioCenterAudioManager.PlayBGM("BGM", "MainMenu");

// 直接傳入 AudioClip
AudioCenterAudioManager.PlayBGM(myClip);

// 淡出
AudioCenterAudioManager.FadeOutBgm(duration: 1.5f);
```

### 步驟三：播放音效

```csharp
// 從音效庫播放
AudioCenterAudioManager.PlaySound("SFX", "Click_Normal");

// 啟用隨機音調變化
AudioCenterAudioManager.PlaySound("Combat", "Gun",
    rndPitch: true,
    rndRange: new Vector2(-0.2f, 0.2f));
```

### 步驟四：使用 Inspector 元件（免程式碼）

在物件上掛載 `AudioCenterAudioController`，於 Inspector 設定 `AudioCenterAudioAction`，
再透過 UnityEvent 呼叫 `DoAction()` 即可。

---

## 核心元件

### AudioCenterAudioManager

> 路徑：`Runtime/Core/AudioCenterAudioManager.cs`

場景中的單例管理器，所有音效操作皆透過靜態方法呼叫。

**Inspector 欄位**

| 欄位 | 類型 | 說明 |
|------|------|------|
| Library | AudioCenterClipLibrary | 音效資料庫（預設使用 DefaultClipLibrary） |
| Default Settings | AudioCenterAudioSettingsSO | 遊戲預設音量與設定 |

---

### AudioCenterAudioController

> 路徑：`Runtime/Core/AudioCenterAudioController.cs`
> 元件選單：`audioCenter/audioCenterAudioController`

由 Inspector 驅動的音效觸發元件，搭配 `AudioCenterAudioAction` 進行設定。為一般的逐物件元件（`MonoBehaviour`），可同時掛載於多個物件上。實際播放交由 `AudioCenterAudioManager` 單例處理；若需從程式碼任意處觸發動作，請呼叫 `AudioCenterAudioManager.DoAction(action)`。

**欄位**

| 欄位 | 說明 |
|------|------|
| Action On Start | 勾選後於 `Start()` 自動執行動作 |
| Audio Action | 設定音效行為（類型、剪輯、是否循環、淡入淡出等） |

**公開方法**

```csharp
audioController.DoAction(); // 手動觸發動作
```

---

### AudioCenterAudioAction（可序列化資料）

**音源類型（AudioCenterAudioSourceType）**

| 值 | 說明 |
|----|------|
| BGM | 背景音樂操作 |
| Sound | 音效操作 |

**BGM 動作（AudioCenterBgmActionType）**

| 值 | 說明 |
|----|------|
| Play | 播放 BGM |
| Stop | 停止 BGM |
| Pause | 暫停 BGM |
| Resume | 繼續播放（可設定淡入） |
| FadeIn | 淡入 |
| FadeOut | 淡出 |

**音效動作（AudioCenterSoundActionType）**

| 值 | 說明 |
|----|------|
| Play | 播放音效 |
| Stop | 停止指定音效 |
| StopAll | 停止所有音效 |
| AttachOnBGM | 與 BGM 同步起始時間播放 |

**播放模式（AudioCenterPlaySoundMode）**

| 值 | 說明 |
|----|------|
| ReplayIfExisted | 若已在播放則重新播放（預設） |
| IgnoreIfExisted | 若已在播放則忽略此次呼叫 |
| AllowMultiple | 允許多個實例同時播放 |

---

### AudioCenterAudioData（設定資料）

```csharp
public class AudioCenterAudioData
{
    public bool  isMuteBgm;      // 靜音背景音樂
    public bool  isMuteSound;    // 靜音所有音效
    public float musicVolume;    // BGM 音量（0 ～ 1）
    public float sfxVolume;      // SFX 音量（0 ～ 1）
    public float uiVolume;       // UI 音效音量（0 ～ 1）
}
```

---

## 音效庫系統

### AudioCenterClipLibrary

> 路徑：`Runtime/Library/AudioCenterClipLibrary.cs`
> 建立方式：`右鍵 → 建立 → AudioCenter → Audio → Clip Library`

單一 `.asset` 檔案內嵌所有群組與剪輯，不需要額外的子資產。

**資料結構**

```
AudioCenterClipLibrary
 ├─ 群組：BGM
 │   ├─ Battle       → Battle.mp3
 │   ├─ MainMenu     → MainPage.mp3
 │   └─ ...
 ├─ 群組：SFX
 │   ├─ Click_Normal → Click_Normal.wav
 │   └─ ...
 └─ 群組：Combat
     ├─ Win          → Win.wav
     └─ ...
```

---

### AudioCenterAudioSettingsSO

> 路徑：`Runtime/Settings/AudioCenterAudioSettingsSO.cs`
> 建立方式：`右鍵 → 建立 → AudioCenter → Audio → Game Settings`

每款遊戲各建立一份，用於設定預設音量及音源池大小。

| 欄位 | 預設值 | 說明 |
|------|--------|------|
| defaultMusicVolume | 0.8 | 預設 BGM 音量 |
| defaultSfxVolume | 1.0 | 預設 SFX 音量 |
| defaultUIVolume | 1.0 | 預設 UI 音效音量 |
| startMuted | false | 啟動時是否全部靜音 |
| poolSize | 10 | 內建音源池初始大小 |
| poolCanExpand | true | 音源池是否可動態擴展 |

---

## 互動音效層

### AudioCenterAmbientAudio

> 路徑：`Runtime/Interactive/AudioCenterAmbientAudio.cs`
> 元件選單：`audioCenter/audioCenterAmbientAudio`

在隨機時間間隔內，從音效池中隨機播放一個音效。
整合原有 `RepeatAudioPlayer` 與 `RandomPlay` 的功能為單一元件。

**欄位**

| 欄位 | 說明 |
|------|------|
| Audio Pool | AudioCenterAudioAction 清單，隨機挑選後透過 AudioCenterAudioManager 播放 |
| Repeat Interval | 播放間隔的隨機範圍（秒），例如 (3, 8) |

**方法**

```csharp
ambientAudio.DoAction();    // 開始循環播放
ambientAudio.StopAction();  // 停止循環
ambientAudio.PlayRandom();  // 手動觸發一次隨機音效
```

---

## 音效設定 UI

### AudioCenterAudioSettings

> 路徑：`Runtime/Settings/AudioCenterAudioSettings.cs`
> 元件選單：`audioCenter/audioCenterAudioSettings`

掛載於設定介面面板，啟動時自動從 `AudioCenterAudioManager` 讀取當前值並同步至滑桿。

**欄位**

| 欄位 | 說明 |
|------|------|
| Music Volume Slider | 控制 BGM 音量的 UI Slider |
| Sfx Volume Slider | 控制 SFX 音量的 UI Slider |
| Ui Volume Slider | 控制 UI 音效音量的 UI Slider |

亦可透過 UnityEvent 或程式碼直接呼叫：

```csharp
audioSettings.UpdateMusicVolume(float volume);
audioSettings.UpdateSfxVolume(float volume);
audioSettings.UpdateUIVolume(float volume);
```

---

## 編輯器工具

### AudioCenter Audio Library 視窗

> 開啟方式：選單列 `AudioCenter → Audio Library`

| 操作項目 | 說明 |
|----------|------|
| Library 欄位 | 切換目前編輯的 AudioCenterClipLibrary 資產 |
| + Group（工具列按鈕） | 新增音效群組 |
| 左側群組清單 | 點擊以切換編輯目標群組 |
| Group Name 欄位 | 重新命名群組 |
| Delete Group 按鈕 | 刪除目前選取的群組 |
| 剪輯清單（可拖曳排序） | 管理群組內的所有音效剪輯 |
| +（清單底部） | 新增剪輯項目 |
| −（清單底部） | 刪除選取的剪輯項目 |

---

### AudioCenterAudioAction 屬性繪製器（PropertyDrawer）

在 Inspector 中智慧顯示 `AudioCenterAudioAction` 欄位：

- 依照類型（BGM／Sound）動態展開對應的設定欄位
- Library 模式下自動顯示群組與剪輯的下拉選單
- 自動搜尋專案中第一個 `AudioCenterClipLibrary` 資產作為來源

---

## API 速查表

```csharp
// ── 背景音樂（BGM）────────────────────────────────────────────
AudioCenterAudioManager.PlayBGM("BGM", "MainMenu");
AudioCenterAudioManager.PlayBGM(audioClip, loop: true);
AudioCenterAudioManager.StopBGM();
AudioCenterAudioManager.PauseBGM();
AudioCenterAudioManager.ResumeBGM(fadeIn: true, fadeInDuration: 1f);
AudioCenterAudioManager.FadeInBgm(duration: 1f);
AudioCenterAudioManager.FadeOutBgm(duration: 1f);
AudioCenterAudioManager.SetBgmDuckMultiplier(0.3f);  // 壓低 BGM（如播放旁白時）
AudioCenterAudioManager.ClearBgmDuckMultiplier();     // 恢復正常 BGM 音量

// ── 音效（SFX）───────────────────────────────────────────────
AudioCenterAudioManager.PlaySound("SFX", "Click_Normal");
AudioCenterAudioManager.PlaySound(audioClip, loop: false);
AudioCenterAudioManager.StopSound(audioClip);
AudioCenterAudioManager.StopAllSound();

// ── 音量設定 ─────────────────────────────────────────────────
AudioCenterAudioManager.UpdateMusicVolume(0.8f);
AudioCenterAudioManager.UpdateSfxVolume(1.0f);
AudioCenterAudioManager.UpdateUIVolume(1.0f);
AudioCenterAudioManager.SetAudioSettings(data);
AudioCenterAudioData data = AudioCenterAudioManager.GetAudioSettings();
```

---

## 音效剪輯對照表

`DefaultClipLibrary.asset` 內建於套件中，涵蓋 b032_boat 專案的全部音效資源。

### BGM 群組

| 剪輯名稱 | 對應音訊檔案 | 使用情境 |
|----------|-------------|---------|
| Battle | Battle.mp3 | PVE 戰鬥 |
| AVG | Journey's Call.mp3 | 劇情／冒險 |
| Opening | Opening.mp3 | 開場動畫 |
| UserRegistry | UserRegistry_party_rock.mp3 | 使用者註冊頁面 |
| Loading | UserRegistry_party_rock_Loop.mp3 | 載入畫面 |
| MainMenu | MainPage.mp3 | 主選單 |
| Shop | MainPage_Shop.mp3 | 商店頁面 |
| Equip | MainPage_Equip.mp3 | 裝備頁面 |
| PVP | MainPage_PVP.mp3 | PVP 大廳 |
| Composite | MainPage_Composite.mp3 | 合成頁面 |
| GameSession | GameSession.mp3 | 遊戲進行中 |
| WeatherUI | Slot.wav | 天氣／槽位 UI |
| TempBattle | PVP_Battle.mp3 | PVP 戰鬥（暫用版本） |
| IncredibleAdventures | incredible_adventures.mp3 | 備用 BGM |

### SFX 群組

| 剪輯名稱 | 對應音訊檔案 | 使用情境 |
|----------|-------------|---------|
| BTN_LetsGo | LestGO.wav | 出發按鈕 |
| Click_Normal | Click_Normal.wav | 一般按鈕點擊 |
| Click_Shop | Click_Shop.wav | 商店按鈕 |
| Click_Equip | Click_Equip.wav | 裝備按鈕 |
| Click_Composite | Click_Composite.wav | 合成按鈕 |
| Click_PVP | Click_PVP.wav | PVP 按鈕 |
| Click_ChestOpen | Click_ChestOpening.wav | 開箱按鈕 |
| Click_Chest | Click_Chest.wav | 寶箱點擊 |
| Click_PopUP | Click_Popup.wav | 彈出視窗開啟 |
| Click_ClosePopUP | Click_ClosePopUP.wav | 彈出視窗關閉 |
| Click_Dong | Click_Normal_Dong.wav | 叮聲確認提示 |
| Click_Buy | Click_Buy.wav | 購買確認 |
| Fail | Fail.wav | 操作失敗提示 |
| Put | Put.wav | 放置物品 |
| PutDown | PutDown.wav | 放下物品（船艦建造） |
| PickUp | PickUp.wav | 拾取物品（船艦移除） |
| PopUPItem | PopUP_ItemList.wav | 物品清單彈出視窗 |
| Slot_Selected | Slot_Selected.wav | 槽位選取 |
| Transition | SceneTransit.wav | 場景切換 |
| TabSwitch_01 | TabSwitch_01.wav | 分頁切換（版本一） |
| TabSwitch_02 | TabSwitch_02.wav | 分頁切換（版本二） |
| LevelUP | Levelup.wav | 升級 |

### Combat 群組

| 剪輯名稱 | 對應音訊檔案 | 使用情境 |
|----------|-------------|---------|
| Win | Win.wav | 勝利 |
| Lose | Lose.wav | 失敗 |
| Bomb | Bom.wav | 爆炸 |
| Drum | Drum.wav | 鼓聲擊打 |
| ShipExplosion | ShipExplosion.wav | 船艦爆炸 |
| MicroExplosion | MicroExplosion.wav | 小型爆炸 |
| Gun | Hit_Gun.wav | 槍擊命中 |

---

*AudioCenter Audio — 為每款 AudioCenter 遊戲提供一致且可擴展的音效基礎建設。*
